#include "iostream"
#include "queue"
#include "vector"

using namespace std;

// 判断拓扑排序是否成立
bool TopSort(vector<vector<int>> &G, int n, vector<int> &inDegree) {
    
}

int main() {
    int n, m;
    cin >> n >> m;

    for (int i = 0 ; i < m; i++) {

    }

    return 0;
}